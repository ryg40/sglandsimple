.env
.env.local
.env.*.local
caddy/Caddyfile.snippet.local
caddy/*.local
__pycache__/
*.pyc
.venv/
# Stage 4 sandbox content — runtime-only, never committed.
sandbox/*
!sandbox/.gitkeep
# Stage 12: Mongo bind-mount data — host-persisted DB files, never committed.
perm/
# Agent runtime / tooling scratch — never committed.
.claude/
.fusion/
