# Changelog

## Unreleased

### Added
- LDAP directory + identity enrichment (Stage 32): added a fixture-backed MCP-side LDAP connector with 200 fake enterprise users, read-only `ldap_lookup_user` / `ldap_lookup_manager_chain` / `identity_enrichment` tools, an authenticated `/api/identity/{user}/enrichment` proxy, and Stage-31 incoming-ticket identity context (reporter/team/manager/title). Identity enrichment now also lists GitHub repos the user has interacted with and maps them to internal applications/environments when known. The auth/RBAC LDAP adapter remains separate and unchanged.
- Standup incoming-ticket triage (Stage 31): `/standup` now conditionally shows an **Incoming tickets** bubble for unassigned/new Jira intake, backed by a read-only `standup_incoming_tickets` MCP tool and authenticated `GET /api/standup/incoming` proxy. The backend extracts AWS/RDS/region/team/user/email/DL entities, matches requests to existing standup workflow templates, and returns connector-hub enrichment/health without writes (optional `STANDUP_INCOMING_DEMO=true` demo rows); the UI renders per-ticket entities, match rationale, connector badges, and a dry-run kickoff affordance.
- Deep Agent HITL approval contract (Stage 21, S21.hitl.1): a write tool listed in an agent profile's `write_tools` now pauses the run at a typed approval (`tool`, args payload, rationale, and the `required_capability` resolved from the owning profile). `agent_run_resume` accepts approve / reject / edited-args, enforces that the resuming actor holds the gating capability before approving or editing a write (web proxy returns **403** otherwise), and — when `DEEP_AGENT_DRY_RUN_ONLY` is on — downgrades an approve to a no-write reject so nothing is applied. Pending approvals survive an MCP restart (run record + LangGraph checkpoint both in Mongo). The `/agents` UI shows the required capability, disables **Approve** for users who lack it, and adds `scripts/smoke_agent_hitl.py`.
- Multi-agent git hygiene enforcement (Stage 30): tracked shared `scripts/git-hooks/pre-commit` and `scripts/install-git-hooks.sh` install `core.hooksPath=scripts/git-hooks` so both Claude Code and PiAgent get the same commit-time syntax guard for staged Python and web TypeScript changes.
- Chat runtime visibility (Stage 26, S26.chat-runtime.1): the `/chat` page now shows a read-only **Runtime routing** panel — the public chat agent's provider, model, and redacted endpoint (host + path only; API keys are never exposed) plus the Deep-Agent orchestrator and per-agent model assignments (with an "inherits default" marker). Values come from a new authenticated `GET /api/chat/runtime` proxy backed by the `chat_runtime_info` MCP tool, not frontend env constants; any chat reader can view it, while admins (`canAdminAuth`) additionally see a clearly-marked, non-functional affordance noting that admin-selectable provider/model routing is planned (validated allowlist + audit log + rollback, no secrets in payloads).
- Standup dry-run gate visibility + admin toggle (Stage 29, S29.gate-toggle.1): the `/standup` config/trace card now shows the effective production-apply gate state (`STANDUP_DRY_RUN_ONLY` plus the read-only MCP-side `WORKFLOW_WRITES_ENABLED`/`JIRA_WRITES_ENABLED`) and an aggregate "live writes effective" indicator. Admins (`canAdminAuth`) get a button to toggle the web-side dry-run gate at runtime (no restart) via a new `GET`/`POST /api/standup/gates` proxy (POST is `canAdminAuth`-gated and audited). The UI states explicitly that the two MCP-side gates are independent and require an MCP restart, so disabling dry-run alone does not enable live writes.
- Deep Agent platform runtime (Stage 21): a `deepagents`-based orchestrator that routes a goal to one of eight system-scoped agents (Atlassian, Mongo, GitHub, ServiceNow, AWS, Audit, Docs, Standup), each with a per-tool allowlist, per-tool HITL gate, and its own model. New MCP tools `agent_profiles_list` and `agent_run_start/status/resume/cancel/artifacts` (runs persist to `deep_agent_runs`) plus web `/api/agents/*` proxies and a `/agents` operations page for listing profiles, starting pollable dry-run runs, inspecting status/artifacts, canceling, and resuming HITL approvals. Agents and tool scopes are declared in `mcp/deep_agent/profiles.yaml`. New env vars: `DEEP_AGENT_PROFILES_FILE`.
- Standup production approvals viewport (Stage 25): approvers can edit staged proposal payloads, save without applying, and submit through gated production apply paths. New env var: `STANDUP_APPROVER_EMAILS` (defaults to `simone.patel@lanGarland.com`).
- Standup reference rail (Stage 24): collapsed Epics and Templates cards on `/standup`, a read-only `/api/standup/epics` proxy, and a backend-owned `standup_templates` MCP tool shared by the UI and Deep Agent context packs. New env vars: `STANDUP_EPICS_ACTIVE_ONLY`, `STANDUP_EPICS_LIMIT`, `STANDUP_TEMPLATES_ENABLED`.
- Optional `sandbox` Deep Agent runtime container (Stage 21), gated behind the `sandbox` compose profile and off by default. Start with `docker compose --profile sandbox up -d`; it runs as a non-root user and shares the `./sandbox` mount with the MCP service. New env vars: `DEEP_AGENT_RUNTIME_MODE`, `DEEP_AGENT_ARTIFACT_DIR`, `DEEP_AGENT_DRY_RUN_ONLY`.

### Changed
- Standup `/standup` chat is now widenable (Stage 27): a **Widen/Collapse** toggle in the chat header triples the live chat column (from the `23rem` rail to `69rem`) for easier screen-share viewing and collapses it back to the right rail. The main viewport section is reordered to **Jira Explorer → Epics + Templates (two-up grid) → Approvals viewport → Jira Configuration / tool trace** (moved to the bottom); the Approvals viewport gained an inline **How to use this** guide explaining the capture → Summarize → edit → Save/Submit dry-run flow. Chat send, presence, and Summarize behave identically in both widths.
- Standup `/standup` layout now keeps live chat at the top of the right-hand column and moves the Stage-24 Templates reference card to the bottom of the main viewport under the Jira Explorer/trace area for better screen-share ergonomics.
- Upgraded the MCP service to the LangChain 1.x line (`deepagents` 0.6.3, `langchain-core` 1.4, `langgraph` 1.2, `langchain-openai` 1.2, `openai` 2.x, `langgraph-checkpoint-mongodb` 0.4) in preparation for the Stage-21 Deep Agent platform. The Mongo checkpointer now uses the unified `MongoDBSaver`; existing `ask_data`, `deep_agent`, docs-agent HITL, agent tool-loop, and workflow smokes pass unchanged.

- Fixed Standup chat send rendering so sender-side optimistic messages are acknowledged in place (`sending` → `sent`) instead of duplicating when the websocket broadcast returns.
- Fixed the Compliance Hub Confluence card so disabled live Atlassian MCP gates with seeded sample pages display as dry-run Confluence evidence instead of the misleading generic "Not Connected" summary.
- Documented the distinction between Confluence live health and dry-run overlap-chain sample data in `docs/mcp-in-this-stack.md`.
